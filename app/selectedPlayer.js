// Selected-V: selected player remove
// document.getElementById('selected-list').addEventListener('click', function(selectedPlayer){
//     selectedPlayer.target.parentNode.removeChild(selectedPlayer.target);
// });

// const allPlayerName = [];
// const cardBtn = document.querySelectorAll('#players-card button');
// for()

// const listContainer = document.getElementById('selected-list');
// listContainer.list
// console.log(listContainer);

// Player Name add event listener
document.getElementById('btn-player-1').addEventListener('click', function(){
    const listContainer = document.getElementById('selected-list');

    const li = document.createElement('li');
    li.innerText = playerCardName('btn-player-1');
    // li.classList.add('');
    listContainer.appendChild(li);

    disableButton('btn-player-1');
});

document.getElementById('btn-player-2').addEventListener('click', function(){
    const listContainer = document.getElementById('selected-list');

    const li = document.createElement('li');
    li.innerText = playerCardName('btn-player-2');
    // li.classList.add('');
    listContainer.appendChild(li);

    disableButton('btn-player-2');
});

document.getElementById('btn-player-3').addEventListener('click', function(){
    const listContainer = document.getElementById('selected-list');

    const li = document.createElement('li');
    li.innerText = playerCardName('btn-player-3');
    // li.classList.add('');
    listContainer.appendChild(li);

    disableButton('btn-player-3');
});

document.getElementById('btn-player-4').addEventListener('click', function(){
    const listContainer = document.getElementById('selected-list');

    const li = document.createElement('li');
    li.innerText = playerCardName('btn-player-4');
    // li.classList.add('');
    listContainer.appendChild(li);

    disableButton('btn-player-4');
});

document.getElementById('btn-player-5').addEventListener('click', function(){
    const listContainer = document.getElementById('selected-list');

    const li = document.createElement('li');
    li.innerText = playerCardName('btn-player-5');
    // li.classList.add('');
    listContainer.appendChild(li);

    disableButton('btn-player-5');
});

document.getElementById('btn-player-6').addEventListener('click', function(){
    const listContainer = document.getElementById('selected-list');

    const li = document.createElement('li');
    li.innerText = playerCardName('btn-player-6');
    // li.classList.add('');
    listContainer.appendChild(li);

    disableButton('btn-player-6');
});

document.getElementById('btn-player-7').addEventListener('click', function(){
    const listContainer = document.getElementById('selected-list');

    const li = document.createElement('li');
    li.innerText = playerCardName('btn-player-7');
    // li.classList.add('');
    listContainer.appendChild(li);

    disableButton('btn-player-7');
});

document.getElementById('btn-player-8').addEventListener('click', function(){
    const listContainer = document.getElementById('selected-list');

    const li = document.createElement('li');
    li.innerText = playerCardName('btn-player-8');
    // li.classList.add('');
    listContainer.appendChild(li);

    disableButton('btn-player-8');
});

document.getElementById('btn-player-9').addEventListener('click', function(){
    const listContainer = document.getElementById('selected-list');

    const li = document.createElement('li');
    li.innerText = playerCardName('btn-player-9');
    // li.classList.add('');
    listContainer.appendChild(li);

    disableButton('btn-player-9');
});

document.getElementById('btn-player-10').addEventListener('click', function(){
    const listContainer = document.getElementById('selected-list');

    const li = document.createElement('li');
    li.innerText = playerCardName('btn-player-10');
    // li.classList.add('');
    listContainer.appendChild(li);

    disableButton('btn-player-10');
});

document.getElementById('btn-player-11').addEventListener('click', function(){
    const listContainer = document.getElementById('selected-list');

    const li = document.createElement('li');
    li.innerText = playerCardName('btn-player-11');
    // li.classList.add('');
    listContainer.appendChild(li);

    disableButton('btn-player-11');
});

document.getElementById('btn-player-12').addEventListener('click', function(){
    const listContainer = document.getElementById('selected-list');

    const li = document.createElement('li');
    li.innerText = playerCardName('btn-player-12');
    // li.classList.add('');
    listContainer.appendChild(li);

    disableButton('btn-player-12');
});